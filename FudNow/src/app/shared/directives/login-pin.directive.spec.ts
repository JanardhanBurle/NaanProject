import { LoginPinDirective } from './login-pin.directive';

describe('LoginPinDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginPinDirective();
    expect(directive).toBeTruthy();
  });
});
